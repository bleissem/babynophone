<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Unity;assembly=Prism.Unity.Forms"
             x:Class="babynophone.App.Views.ChooseSkypeUserPage">
    <StackLayout>
        <Label Text="{Binding ChooseSkypeUserLabel, Mode=OneWay}" />
        <Entry Text="{Binding SkypeUser, Mode=TwoWay}" />
        <Label Text="{Binding UseVideoLabel, Mode=OneWay}" />
        <Switch IsToggled="{Binding UseVideo, Mode=TwoWay}" />
        <Button Text="{Binding ChooseSkypeUserOKButton}" Command="{Binding ChooseSkypeUserOKButtonCommand}" />
        <Label Text="{Binding SkypeIsInstalledText}" />
    </StackLayout>
</ContentPage>